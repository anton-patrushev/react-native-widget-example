import { Colors } from 'modules/shared/presentation/colors/Colors';
import { StyleSheet } from 'react-native';

export const AsyncButtonStyle = StyleSheet.create({
  buttonView: {
    justifyContent: 'center',
    alignItems: 'center',
    height: 40,
  },
  label: {
    color: Colors.WHITE,
  },
});
